<%= simple_form_for(@proposal, defaults: { wrapper_html: {class: 'form-group'}, input_html: { class: 'form-control' } }) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>
